name: GPUMonitor
options:
  bundleIdPrefix: com.gpumonitor
  createIntermediateGroups: true
  deploymentTarget:
    macOS: "14.0"
    iOS: "17.0"
    watchOS: "10.0"
    tvOS: "17.0"

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "16"
    SWIFT_VERSION: "5.9"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: DQPDVF6ZSY

schemes:
  GPUMonitor-macOS:
    build:
      targets:
        GPUMonitor-macOS: all
  GPUMonitor-iOS:
    build:
      targets:
        GPUMonitor-iOS: all
  GPUMonitor-watchOS:
    build:
      targets:
        GPUMonitor-watchOS: all
  GPUMonitor-tvOS:
    build:
      targets:
        GPUMonitor-tvOS: all
  GPUMonitorWidgetApp:
    build:
      targets:
        GPUMonitorWidgetApp: all
  gpu-cli:
    build:
      targets:
        gpu-cli: all

targets:

  # ──────────────────────────────────────────────
  # macOS App
  # ──────────────────────────────────────────────
  GPUMonitor-macOS:
    type: application
    platform: macOS
    sources:
      - Shared
      - Platforms/macOS/Views
      - servers.json
      - Platforms/macOS/Assets.xcassets
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.macos
      PRODUCT_NAME: GPU Wattage Meters
      INFOPLIST_FILE: Platforms/macOS/Info.plist
      MACOSX_DEPLOYMENT_TARGET: "14.0"
      COMBINE_HIDPI_IMAGES: true
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      ENABLE_HARDENED_RUNTIME: YES
      CODE_SIGN_ENTITLEMENTS: Platforms/macOS/GPUMonitor.entitlements

  # ──────────────────────────────────────────────
  # iOS App
  # ──────────────────────────────────────────────
  GPUMonitor-iOS:
    type: application
    platform: iOS
    sources:
      - Shared
      - Platforms/iOS/Views
      - servers.json
      - Platforms/iOS/Assets.xcassets
    dependencies:
      - target: GPUMonitor-watchOS
        embed: true
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.ios
      PRODUCT_NAME: GPU Wattage Meters
      INFOPLIST_FILE: Platforms/iOS/Info.plist
      IPHONEOS_DEPLOYMENT_TARGET: "17.0"
      TARGETED_DEVICE_FAMILY: "1,2"
      SUPPORTS_MACCATALYST: false
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

  # ──────────────────────────────────────────────
  # watchOS App
  # ──────────────────────────────────────────────
  GPUMonitor-watchOS:
    type: application
    platform: watchOS
    sources:
      - Shared
      - Platforms/watchOS/Views
      - servers.json
      - Platforms/watchOS/Assets.xcassets
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.ios.watchkitapp
      PRODUCT_NAME: GPU Wattage Meters
      INFOPLIST_FILE: Platforms/watchOS/Info.plist
      WATCHOS_DEPLOYMENT_TARGET: "10.0"
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      SKIP_INSTALL: true
      SUPPORTS_MACCATALYST: false

  # ──────────────────────────────────────────────
  # tvOS App
  # ──────────────────────────────────────────────
  GPUMonitor-tvOS:
    type: application
    platform: tvOS
    sources:
      - Shared
      - Platforms/tvOS/Views
      - servers.json
      - Platforms/tvOS/Assets.xcassets
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.ios
      PRODUCT_NAME: GPU Wattage Meters
      INFOPLIST_FILE: Platforms/tvOS/Info.plist
      TVOS_DEPLOYMENT_TARGET: "17.0"
      ASSETCATALOG_COMPILER_APPICON_NAME: "App Icon"

  # ──────────────────────────────────────────────
  # macOS Widget Host App
  # ──────────────────────────────────────────────
  GPUMonitorWidgetApp:
    type: application
    platform: macOS
    sources:
      - Widget/App
      - servers.json
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.widget
      PRODUCT_NAME: GPU Wattage Meters Widget
      INFOPLIST_FILE: Widget/App/Info.plist
      MACOSX_DEPLOYMENT_TARGET: "14.0"
      COMBINE_HIDPI_IMAGES: true
    entitlements:
      path: Widget/App/GPUMonitorWidgetApp.entitlements
    dependencies:
      - target: GPUMonitorWidgetExtension

  # ──────────────────────────────────────────────
  # macOS Widget Extension
  # ──────────────────────────────────────────────
  GPUMonitorWidgetExtension:
    type: extensionkit-extension
    platform: macOS
    sources:
      - Widget/Extension
      - servers.json
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.widget.extension
      PRODUCT_NAME: GPUMonitorWidgetExtension
      INFOPLIST_FILE: Widget/Extension/Info.plist
      MACOSX_DEPLOYMENT_TARGET: "14.0"
      SKIP_INSTALL: true
      LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks @executable_path/../../../../Frameworks"
    entitlements:
      path: Widget/Extension/GPUMonitorWidgetExtension.entitlements

  # ──────────────────────────────────────────────
  # CLI Tool
  # ──────────────────────────────────────────────
  gpu-cli:
    type: tool
    platform: macOS
    sources:
      - CLI
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.gpumonitor.cli
      PRODUCT_NAME: gpu-cli
      MACOSX_DEPLOYMENT_TARGET: "14.0"
